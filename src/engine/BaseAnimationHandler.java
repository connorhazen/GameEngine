package engine;

import util.Grid;

public class BaseAnimationHandler implements AnimationHandler {

  @Override
  public GameAnimation getAnimation(Grid currentGrid, Grid updatedGrid) {
    return null;
  }
}
